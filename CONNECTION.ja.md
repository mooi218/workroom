# Workroomに仕事を接続する

[English](CONNECTION.md) · [Workroomに戻る](README.ja.md)

**公開デモに並ぶのは架空の仕事です。自分の仕事を表示するには、ダウンロードしたWorkroomを自分のパソコンで起動します。**

| 表示したい仕事 | 接続方法 |
| --- | --- |
| このPCに保存されたCodexの仕事 | ローカル記録を自動で読む。拡張や接続コードは不要 |
| Chrome・Edgeで開いているChatGPT Work | 任意のブラウザー拡張。対応する開いたページ・選択した仕事だけ |
| 閉じたページや別のPCを含むクラウドの全仕事 | この観測機能の対象外 |

## 1. ローカルのオフィスを起動する

1. [Node.js 24以降](https://nodejs.org/)をインストールします。
2. [Workroomをダウンロード](https://github.com/mooi218/workroom/releases/latest)し、ZIPを展開します。
3. Windowsでは `start-windows.cmd`、macOSでは `start-macos.command` を開きます。起動できない場合は、`package.json` が入った展開先フォルダーでターミナルを開いて実行します。

   ```sh
   npm start
   ```

4. [http://127.0.0.1:4318](http://127.0.0.1:4318) を開き、使う間はターミナルを起動したままにします。デモではなく実際の仕事の表示を選びます。`npm install` は不要です。

起動しない場合は `node --version` を確認します。Nodeを入れた後はターミナルを開き直してください。展開先で `npm start` を実行すれば、起動エラーを確認できます。ローカルのページに接続できない場合はサーバーが動いているか確認します。公開GitHub Pagesデモからローカル記録へ接続する仕組みではありません。

## 2. このPCのCodexを表示する

同じパソコンでCodexを使います。対応する保存済みタスクを、プロジェクトや通常の子タスクをまたいで約2.5秒ごとに読み取ります。接続のために新しい仕事を実行したり、APIキーやブラウザー拡張を用意したりする必要はありません。

Codexの件数を確認します。**「進行中のみ」では完了・未確認の仕事が隠れる場合があります。**「すべて表示」にし、プロジェクト・部署・状態の絞り込みを解除します。仕事の詳細には「最終観測」が表示されます。

ローカル情報を取得できない場合は、このPCのCodexに仕事が保存されているか確認します。既定の保存先は `CODEX_HOME`、未指定ならホームフォルダー内の `.codex` です。別の保存先を使っている場合は、起動時に `--codex-home` でそのフォルダーを指定します。非対応の保存形式は取得不可・未確認として扱い、記録を修復しません。

**仕事は見えるが、使用量や指示が使えない場合：**これらの機能には、インストール済みのCodex CLIのapp-serverと、そこでサインインしているアカウントも必要です。CLI・意図したアカウント・Codex保存先を確認します。WindowsではCodex側のサンドボックス設定を済ませ、設定エラーがあればCodexで確認してください。OSやワークスペースの権限制限は回避しません。仕事のカードが見えることと、このCLI接続の準備ができていることは別の確認です。

## 3. 必要に応じてブラウザーのChatGPT Workも接続する

これは**実験的・非公式な画面観測ツール**です。拡張機能の導入や画面認識を、**実際のChatGPT Workアカウントで検証したものではありません**。クラウドの全件同期APIでもありません。最初は実際の仕事の画面と観測された状態を照合してください。

1. ローカルのWorkroomを **http://127.0.0.1:4318** で開いておきます。拡張はこのPCのアプリへ接続するので、拡張の画面では接続コードだけを入力します。
2. パソコン版Chromeでは `chrome://extensions`、Edgeでは `edge://extensions` をアドレス欄に入力します。**開発者モード**をオンにし、**パッケージ化されていない拡張機能を読み込む**から、Workroomの展開先にある **`extension` フォルダー**を選びます。`manifest.json` が入っているフォルダーです。[Chromeの公式手順](https://developer.chrome.com/docs/extensions/get-started/tutorial/hello-world)、[Edgeの公式手順](https://learn.microsoft.com/en-us/microsoft-edge/extensions/getting-started/extension-sideloading)も確認できます。
3. Workroomで **オフィスの設定 → 接続コードを表示 → コピー**を選びます。ブラウザーの拡張機能メニューから **Workroom — ChatGPT Work Observer** を開き、**Workroom の接続コード**欄へ貼り付けて **保存**を押します。入力するのはこの接続コードだけです。ChatGPTのログイントークン、APIキー、接続先アドレスは入力しません。
4. 同じブラウザーで正しいWorkの仕事を開きます。対象は `chatgpt.com` の `/c/<id>` または `/g/<gpt-id>/c/<id>` 形式の会話ページです。拡張の導入・更新後はページを再読み込みします。拡張に出た仕事名を確認し、**このタスクを追跡**を押します。**Work 表示のあるタスクを自動追跡**は、明示的なWork表示を認識したときだけ働きます。
5. 拡張に **このタスクの表示状態を反映しています**と表示されること、Workroomの**Work件数**、仕事の詳細の**最終観測**を確認します。隠れている場合は「すべて表示」にして絞り込みを解除します。ブラウザー・仕事のタブ・サーバーを起動したままにすると、約30秒ごとにも観測を送ります。

### つまずいた場所を確認する

| 表示・症状 | 確認すること |
| --- | --- |
| 拡張の読み込みボタンがない・失敗する | 開発者モードを有効にし、ZIPや外側のフォルダーではなく展開済みの `extension` を選ぶ。組織が開発者拡張を禁止している場合は管理者へ確認する |
| 接続コードが一致しない | いま起動中のローカルWorkroomからコピーし直して「保存」。別の展開先のコードを混ぜない |
| オフィスに接続できない | Workroomを4318番ポートで起動したままにする。別ポートや公開デモはこの拡張の受信先にならない |
| 追跡ボタンが押せない | 対応する会話タブを選び、コードを保存し、ページを再読み込みして拡張を開き直す。ホームやサイドバーは対象の会話ページではない |
| コードは保存済みだが仕事が出ない | 保存済みは受信済みではない。正しい仕事名を確認して「このタスクを追跡」を選ぶ |
| Workが0件・状態が未確認 | 仕事名、追跡状態、コード、ポート、ページ再読み込みを確認。非対応の状態表示、タブの休止、約90秒以上の観測途切れは未確認になり得る。完了を意味しない |

1件を止めるには **このタスクの追跡を停止**を選びます。その会話の自動追跡も止まり、もう一度追跡を選ぶと再開します。**接続を解除**は拡張とオフィスの接続を解除します。

閉じた・休止した・見えていないクラウドの仕事からは、確かな現在状態を受信できません。1件を選んでも他の会話は選択されません。対応する複数の開いたタブは観測できますが、ボタンが消えただけでは完了と判断しません。[拡張機能の観測範囲とデータの扱い](extension/README.md)も参照してください。

接続コードは拡張の入力欄だけに使い、チャット・公開Issue・スクリーンショットへ載せないでください。観測と表示ではモデルを呼びません。実際のCodex・Workの仕事を実行したり指示を送ったりする場合は、通常の利用量が発生します。
