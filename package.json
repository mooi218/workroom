{
  "name": "workroom-office",
  "version": "0.1.0",
  "private": true,
  "description": "A local pixel office for Codex and observed ChatGPT Work tasks. No model calls.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "demo": "node server.mjs --demo",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/office.js"
  },
  "license": "MIT"
}
